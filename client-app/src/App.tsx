import { Box, Typography } from '@mui/material';
import React from 'react';

const App: React.FC = () => (
  <Box>
    <Typography variant="h3" component="h1">
      Expense Tracker
    </Typography>
  </Box>
);

export default App;
